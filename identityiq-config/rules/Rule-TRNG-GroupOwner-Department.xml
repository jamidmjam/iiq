<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE Rule PUBLIC "sailpoint.dtd" "sailpoint.dtd">
<Rule language="beanshell" name="Group Owner - Manager by Job Title" type="GroupOwner">
  <Description>
    Set the owner of each subgroup to the first Identity in that subgroup with jobtitle equal to "Manager". If none are found, default to spadmin.
  </Description>
  <Signature returnType="Object">
    <Inputs>
      <Argument name="log">
        <Description>The log object associated with the SailPointContext.</Description>
      </Argument>
      <Argument name="context">
        <Description>A sailpoint.api.SailPointContext object that can be used to query the database if necessary.</Description>
      </Argument>
      <Argument name="factory">
        <Description>The GroupFactory that generated the populations.</Description>
      </Argument>
      <Argument name="group">
        <Description>One GroupDefinition from the factory.</Description>
      </Argument>
    </Inputs>
    <Returns>
      <Argument name="owner">
        <Description>The group owner. The name or id of an Identity or the Identity object itself.</Description>
      </Argument>
    </Returns>
  </Signature>
  <Source>
    <![CDATA[
    import sailpoint.object.QueryOptions;
    import sailpoint.object.Identity;

    QueryOptions qo = new QueryOptions();
    qo.addFilter(group.getFilter());

    Iterator identities = context.search(Identity.class, qo);

    Identity emp = null;
    Identity owner = null;

    while (identities.hasNext()) {
      emp = (Identity) identities.next();
      String jobTitle = (String) emp.getAttribute("jobtitle");
      log.debug("Evaluating: " + emp.getName() + " | jobtitle = " + jobTitle);

      if (jobTitle != null && jobTitle.equalsIgnoreCase("Manager")) {
        log.debug("Found manager for group " + group.getName() + ": " + emp.getName());
        owner = emp;
        break;
      }
    }

    if (owner == null) {
      log.debug("No manager found for group: " + group.getName() + ". Defaulting to spadmin.");
      return "spadmin";
    }

    return owner;
    ]]>
  </Source>
</Rule>
