<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE Rule PUBLIC "sailpoint.dtd" "sailpoint.dtd">
<Rule name="Group Owner - Manager by Title" language="beanshell" type="GroupOwner">
  <Description>
    Set the owner of each subgroup to the first Identity whose job title contains 'manager'. If none found, default to spadmin.
  </Description>
  <Signature returnType="Object">
    <Inputs>
      <Argument name="log" />
      <Argument name="context" />
      <Argument name="factory" />
      <Argument name="group" />
    </Inputs>
    <Returns>
      <Argument name="owner" />
    </Returns>
  </Signature>
  <Source>
    <![CDATA[
    import sailpoint.object.QueryOptions;
    import sailpoint.object.Identity;

    QueryOptions qo = new QueryOptions();
    qo.addFilter(group.getFilter());

    Iterator identities = context.search(Identity.class, qo);
    Identity owner = null;

    while (identities.hasNext()) {
      Identity emp = (Identity) identities.next();
      Object titleObj = emp.getAttribute("jobtitle");

      if (titleObj != null && titleObj.toString().toLowerCase().contains("manager")) {
        owner = emp;
        break;
      }
    }

    if (owner == null) {
      log.debug("No manager found for group: " + group.getName() + ". Defaulting to spadmin.");
      return "spadmin";
    }

    log.debug("Assigned owner for group: " + group.getName() + " is: " + owner.getName());
    return owner;
    ]]>
  </Source>
</Rule>

