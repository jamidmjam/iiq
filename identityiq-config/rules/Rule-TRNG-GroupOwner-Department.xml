<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE Rule PUBLIC "sailpoint.dtd" "sailpoint.dtd">
<Rule name="TRNG-GroupOwner-Department" type="GroupOwner" language="beanshell">
  <Description>
    Assigns group ownership based on first manager found in the department.
    Compatible with IIQ BeanShell engine.
  </Description>
  <Source>
    <![CDATA[
      import sailpoint.object.Identity;
      import sailpoint.object.Filter;

      // Get the group object from context
      Object groupObj = ruleContext.get("group");

      // Use reflection-safe getter for group value
      String department = null;
      if (groupObj != null) {
          try {
              department = groupObj.getClass().getMethod("getValue", (Class[]) null).invoke(groupObj, (Object[]) null);
          } catch (Exception e) {
              log.error("Could not retrieve group value from group object: " + e);
          }
      }

      if (department == null || department.isEmpty()) {
          return null;
      }

      // Build filter to find managers in this department
      Filter filter = Filter.and(
        Filter.eq("department", department),
        Filter.like("title", "%Manager%")
      );

      // Query the identities
      List results = context.getObjects(Identity.class, filter);

      if (results != null && results.size() > 0) {
          return results.get(0);
      }

      return null;
    ]]>
  </Source>
</Rule>

