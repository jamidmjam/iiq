<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE Rule PUBLIC "sailpoint.dtd" "sailpoint.dtd">
<Rule name="SetNativePasswordRule" type="IdentityCreation">
  <Description>
    Sets the native SailPoint password to "Password123" when a new Identity is created.
  </Description>
  <Source>
    <![CDATA[
    import sailpoint.object.Identity;

    if (identity != null) {
        log.info("SetNativePasswordRule: Setting native password for identity: " + identity.getName());
        identity.setPassword("Password123");
    } else {
        log.warn("SetNativePasswordRule: Identity object is null.");
    }

    return identity;
    ]]>
  </Source>
</Rule>

